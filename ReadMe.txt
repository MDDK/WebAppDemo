Web app expects a MySql database at localhost:3306 called demodb
with a user: root password: sqldemo10 to connect to the database.
Database Dump is included.

On the Login.jsp default page the web app should accept User: Admin pass: Admin and user: User pass: User
Alternatively create your own user with chosen privilages.

